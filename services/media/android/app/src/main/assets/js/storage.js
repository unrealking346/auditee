const WaeveStore=(()=>{const KEY='waeve.production.v1';const defaults={user:null,region:'Global',favorites:[],favoriteArtists:[],likedReleases:[],likedPlaylists:[],recent:[],playlists:[],stats:{plays:0,seconds:0,completed:0},shuffle:false,repeat:'off',volume:.8,muted:false,playbackRate:1,premium:false,trialUsed:false,settings:{autoplay:true,explicit:true,quality:'High'},notifications:[]};let s=load();function load(){try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...defaults}}}function save(){localStorage.setItem(KEY,JSON.stringify(s))}function get(){return s}function set(k,v){s[k]=v;save()}function notify(text){s.notifications.unshift({id:Date.now(),text,read:false});s.notifications=s.notifications.slice(0,30);save()}function register(user){s.user=user;s.region=user.region||'Global';save()}function logout(){s.user=null;save()}function del(){localStorage.removeItem(KEY);location.reload()}function recent(id){s.recent=[id,...s.recent.filter(x=>x!==id)].slice(0,50);save()}function stat(k,v=1){s.stats[k]=(s.stats[k]||0)+v;save()}function toggleIn(k,id){const a=s[k];const i=a.indexOf(id);i>=0?a.splice(i,1):a.push(id);save();return i<0}return{get,set,save,notify,register,logout,del,recent,stat,toggleIn,isFavorite:id=>s.favorites.includes(id),toggleFavorite:id=>toggleIn('favorites',id),isArtistFavorite:id=>s.favoriteArtists.includes(id),toggleArtistFavorite:id=>toggleIn('favoriteArtists',id),isReleaseLiked:id=>s.likedReleases.includes(id),toggleReleaseLike:id=>toggleIn('likedReleases',id),isPlaylistLiked:id=>s.likedPlaylists.includes(id),togglePlaylistLike:id=>toggleIn('likedPlaylists',id)}})();
